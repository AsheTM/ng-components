import { ngWalletComponents } from './ng-wallet-components';

describe('ngWalletComponents', () => {
  it('should work', () => {
    expect(ngWalletComponents()).toEqual('ng-wallet-components');
  });
});
